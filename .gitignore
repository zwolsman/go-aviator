# SSH host key — mount as a k8s secret, never commit
keys/host_ed25519
keys/host_ed25519.pub

# Build output
/aviator
/server

# Environment files
.env
.env.*
!.env.example

# Go tooling
vendor/

# OS
.DS_Store
