services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: crashgame
      POSTGRES_USER: crashgame
      POSTGRES_PASSWORD: crashgame
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U crashgame"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
